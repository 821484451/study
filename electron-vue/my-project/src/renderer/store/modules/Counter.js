const state = {
}

const mutations = {
  DECREMENT_MAIN_COUNTER (state) {
  },
  INCREMENT_MAIN_COUNTER (state) {
  }
}

const actions = {
  someAsyncTask ({ commit }) {
  }
}

export default {
  state,
  mutations,
  actions
}
